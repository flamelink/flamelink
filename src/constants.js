export const DEFAULT_CONFIG = {
  env: 'production',
  locale: 'en-US'
};

export const ALLOWED_CHILD_EVENTS = [
  'value',
  'child_added',
  'child_removed',
  'child_changed',
  'child_moved'
];

export const DEFAULT_REQUIRED_IMAGE_SIZE = 240;
